# ── Database ────────────────────────────────────────────────────────────────
# Local development: the API runs on your machine, Postgres on localhost.
# docker-compose.dev.yml / docker-compose.prod.yml override DATABASE_URL via
# `environment:` so containers always use their own "postgres" service.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/buildup?schema=public

NODE_ENV=development
HOST=0.0.0.0
PORT=3000

# ── Auth ────────────────────────────────────────────────────────────────────
# Two DIFFERENT random strings, at least 32 characters each (the API refuses to
# start in production otherwise). Generate with: openssl rand -base64 48
JWT_SECRET=change-me
JWT_REFRESH_SECRET=change-me-too
JWT_ACCESS_TOKEN_EXPIRATION=15m
JWT_REFRESH_TOKEN_EXPIRATION=7d

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# ── E-mail (Resend) ─────────────────────────────────────────────────────────
RESEND_API_KEY=
EMAIL_FROM=noreply@buildup.ge

# ── API docs ────────────────────────────────────────────────────────────────
# Swagger UI (/api/docs) is always on in development; opt in for production
ENABLE_SWAGGER=false

# ── Frontend ────────────────────────────────────────────────────────────────
# Used for e-mail links, Google OAuth redirects and CORS
FRONTEND_URL=http://localhost:3001
# Extra allowed CORS origins, comma separated (optional)
CORS_ORIGINS=

# ── Admin ───────────────────────────────────────────────────────────────────
# Account that manages developers / projects / leads. Sign up with this e-mail,
# then run `npx prisma db seed` to promote it to ADMIN.
ADMIN_EMAIL=

# ── Listings ────────────────────────────────────────────────────────────────
# Fallback phone for listings whose owner has none (optional; seeded into site_settings)
DEFAULT_CONTACT_PHONE=

# Where admin alerts (new leads, listings awaiting review) are sent; defaults to ADMIN_EMAIL
ADMIN_NOTIFY_EMAIL=
